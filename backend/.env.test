# Test environment (integration tests via docker-compose.test.yml)
# Non-sensitive values: only used by Jest against a disposable test database.
DATABASE_URL=postgres://test:test@localhost:5434/blablabook_test
JWT_SECRET=test-secret-not-used-in-production-must-be-at-least-32-characters-long
NODE_ENV=test
